{"note":"Don't delete this file! It's used internally to help with page regeneration.","name":"Hawkthorne Multiplayer","google":"","tagline":"","body":"# Multiplayer Client/Server interface\r\n\r\n## Client to Server\r\n\r\nNote:\r\nthe **entity** is an id assigned to the player when he tries to register.\r\n**obj** and **plyr** sent through the udp sockets are [packed representations](http://nimbusbp1729.github.com/hawkthorne-server-lua/packed.html) of an object and a player respectively.\r\n\r\nSupported commands and when the should be sent:\r\n\r\nwhen the client presses a button\r\n\r\n    (entity) keypressed (button)\r\n\r\nwhen the client releases a button\r\n\r\n    (entity) keyreleased (button)    \r\n\r\nwhen the client requests an update of level `level_name`\r\nNote: it is expected that the server responds with `updatePlayer` and `updateObject`\r\n\r\n    (entity) update (levelName)\r\n\r\nwhen the client registers a character by the name of `name` with costume `costume`    \r\n\r\n    (entity) register (name) (costume)\r\n\r\nTODO: registration response with entity assignment\r\n\r\nwhen the client decides to enter a specific level\r\n\r\n    (entity) enter (level)    \r\n\r\nTODO: character costume switch\r\n\r\n## Server to Client\r\n\r\nwhen the server gets a request from an entity to `update`\r\nthis is sent to a single client\r\n\r\n    (entity) updatePlayer (plyr)\r\n\r\nwhen the server gets a request from an entity to     update    \r\nthis is sent to a single client\r\n\r\n    (entity) updateObject (obj)    \r\n\r\nwhen the server notices a player switching between backend gamestates\r\nthis is sent to all clients\r\n\r\n    (entity) stateSwitch (fromLevel) (toLevel)    \r\n\r\nwhen the server wants to tell a client to make a sound\r\nthis may or may not be sent to all clients\r\n\r\n    (entity) sound (soundFile)"}